spring:
  datasource:
    url: jdbc:mysql://localhost:3306/travel?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 10521004ss!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

jwt:
  secret: "CHANGE_ME_TO_A_LONG_RANDOM_SECRET_KEY_256BIT"
  accessExpiration: 900000      # 15분
  refreshExpiration: 1209600000 # 14일
app:
  providers:
    kakao:
      rest-api-key: ${KAKAO_REST_API_KEY}
      base-url: https://dapi.kakao.com
    google:
      api-key: ${GOOGLE_API_KEY}
      base-url: https://maps.googleapis.com

odsay:
  apiKey: ${ODSAY_API_KEY}
  baseUrl: https://api.odsay.com/v1/api
  connectTimeoutMs: 3000
  readTimeoutMs: 5000

server:
  port: 8080

