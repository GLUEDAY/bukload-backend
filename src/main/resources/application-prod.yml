spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/bukload?useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: younsi1314**   # 실제 MySQL 비밀번호로 수정

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update   # 처음엔 update로 두고, 나중에 데이터 정착 후 none으로 변경
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8081

ai:
  gemini:
    endpoint: https://generativelanguage.googleapis.com/v1
    model-region: gemini-2.5-flash
    model-courses: gemini-2.5-flash
    api-key: ${GEMINI_API_KEY}
